export default class LazyLoadSrc {
    private readonly DATA_ATTR;
    private count;
    private items;
    private max;
    constructor();
    private unbracket(str);
    private filterEmpty();
    private addListener();
    private lazyLoaded();
    private callNext();
}
