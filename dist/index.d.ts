export default class LazyLoadSrc {
    private readonly DATA_ATTR;
    private count;
    private max;
    private items;
    constructor();
    private unbracket(str);
    private filterEmpty();
    private addListener();
    private lazyLoaded();
    private callNext();
}
